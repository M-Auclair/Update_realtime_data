name: Update Realtime Water Level Data

on:
  schedule:
    # Run every 2 hours on fixed sched between 6am and 8pm MT
    - cron: '0 12 * * *' # 6:00 AM MT daily
    - cron: '0 14 * * *'  # 8:00 AM MT daily
    - cron: '0 16 * * *'  # 10:00 AM MT
    - cron: '0 18 * * *' # 12:00 PM MT daily
    - cron: '0 20 * * *' # 14:00
    - cron: '0 22 * * *' # 16:00
    - cron: '0 0 * * *' # 18:00
    - cron: '0 2 * * *' # 20:00
  workflow_dispatch:  # Allows manual triggering from GitHub Actions tab

jobs:
  update-data:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: write  # Required to push commits
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        fetch-depth: 0  # Fetch all history for proper git operations
    
    - name: Set up R
      uses: r-lib/actions/setup-r@v2
      with:
        r-version: '4.5.2'  
    
    - name: Install R dependencies
      uses: r-lib/actions/setup-r-dependencies@v2
      with:
        packages: |
          tidyhydat
          dplyr
          lubridate
        needs: |
          checkout
          setup-r
    
    - name: Run data update script
      timeout-minutes: 45
      run: |
        Rscript scripts/update_realtime_data.R
      env:
        TZ: UTC
    
    - name: Commit and push if changed
      if: success()
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add data/realtime_WL_data.rds
        git diff --staged --quiet || (git commit -m "Auto-update: Realtime water level data $(date +'%Y-%m-%d %H:%M:%S UTC')" && git push)